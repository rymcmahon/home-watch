<div class="container">
  <div class="sign-in">
    <% if user_signed_in? %>
      <%= link_to('Log Out', destroy_user_session_path, :class => "btn btn-default btn-lg", :method => :delete) %>
    <% else %>
    <%= link_to "Sign In", new_user_session_path, class:"btn btn-default btn-lg" %>
    <% end %>
  </div>
</div>
